# V4.7 真实验收清单

状态：`NOT TESTED`（本轮遵循用户要求，不使用电脑控制工具）。

代码和离屏自动化覆盖了启动、动态角色预览/重置、缩放、语义动画、工作日历月格、节假日详情、工资设置和隐私脱敏。以下项目需要在真实 Windows 桌面由用户手动确认：

- 角色拖动、气泡与右侧/边缘屏幕定位
- 资源管理器拖入、创建/删除/重命名事件动画
- 真实字体下的工作日历与工资设置视觉层次、无标题栏拖动
- 调休日期（2026-09-20、2026-10-10）与休息日的颜色、文案和来源链接
- EXE 全新解压副本启动、日志生成及退出

验收临时文件统一放在 `D:\pet-desktop\.tmp\v47-acceptance\`，不写入用户主目录。
